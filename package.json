{
  "name": "web-ar",
  "version": "1.0.0",
  "description": "A repo for playing around with web based AR using AR.js",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
